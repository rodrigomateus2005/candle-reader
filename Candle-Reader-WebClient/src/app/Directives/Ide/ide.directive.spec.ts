import { IdeDirective } from './ide.directive';

describe('IdeDirective', () => {
  it('should create an instance', () => {
    const directive = new IdeDirective();
    expect(directive).toBeTruthy();
  });
});
